import io;
import pedro;

class Fac extends Pedro{
    int andre=10;
    public int computeFac(int num, int tmp){
        int num_aux;
        if ( num < 1)
            num_aux = 1
        else
            num_aux = num * ( this.computeFac(num -1));
        return num_aux;
    }

    public static void main(String[] args){
        int pedro = 10;
        io.println(new Fac().computeFac(10));
    }
}
