import Simple;
import io;

class Fac {
    public int compFac(Simple param) {
        Simple local = new Simple();
        int a;
        int b;
        int return_value;
        param = local;
        a = 20;
        b = 10;
        return_value = param.add(a, b);
        return return_value;
    }

    public static void main(String[] args) {
        int temp_res = (new Fac()).compFac(new Simple());
        io.println(temp_res);
    }
}